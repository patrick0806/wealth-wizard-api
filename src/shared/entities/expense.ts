export class Expense {
  id: string;
  description: string;
  category: string;
  origin: string;
  installments: number;
  installmentValue: number;
  totalValue: number;
  initialDate: Date;
  finishDate: Date;
}
